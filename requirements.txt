# Core numerics
numpy>=1.24
pandas>=2.0
scipy>=1.11

# Deep learning (CPU build — no CUDA required)
torch>=2.1 --index-url https://download.pytorch.org/whl/cpu

# ML (conformal calibration — GBM quantile regression)
scikit-learn>=1.3

# Panel econometrics (Fama-MacBeth)
linearmodels>=5.3

# Parquet I/O
pyarrow>=13.0

# Configuration
pyyaml>=6.0

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Progress bars (essential for long CPU training runs)
tqdm>=4.65

# Jupyter (exploratory notebooks)
jupyter>=1.0
ipykernel>=6.0
